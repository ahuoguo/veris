name: Verify with Verus

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Download Verus
        run: |
          wget https://github.com/verus-lang/verus/releases/download/release%2F0.2026.01.23.1650a05/verus-0.2026.01.23.1650a05-x86-linux.zip
          unzip verus-0.2026.01.23.1650a05-x86-linux.zip
      - name: Add Verus to PATH
        run: echo "$(pwd)/verus-x86-linux/" >> $GITHUB_PATH
      
      - name: Run opendp.sh
        run: bash opendp.sh
      
      - name: Run verify.sh
        run: |
          cd ub
          bash verify.sh